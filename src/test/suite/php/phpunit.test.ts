import * as assert from "assert";
import * as vscode from "vscode";
import { join } from "path";
import { waitToAssertInSeconds, timeout } from "../extension.test";

describe("PHP PHPUnit tests", () => {
  it("Runs tests for an entire file", async () => {});

  it("Runs the test under the cursor", async () => {});

  it("Runs the entire test suite", async () => {});

  it("Runs the previous test", async () => {});

  it("Runs the previous test, even if in another file", async () => {});
});

import * as assert from "assert";
import * as vscode from "vscode";

describe("JavaScript Mocha tests", () => {
  it("Runs tests for an entire file", () => {});
  it("Runs the test under the cursor", () => {});
  it("Runs the entire test suite", () => {});
  it("Runs the previous test", () => {});
  it("Runs the previous test, even if in another file", () => {});
});
